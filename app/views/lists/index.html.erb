<div class="container">
  <h1>Lists</h1>
  <div class="card">
    <ul class="list-groupe">
    <% @lists.each do |list| %>
      <li class="list-group-item"><h2><%= list.name %></h2>
      <%= link_to 'Show list', list_path(list), style: "text-decoration:none" %></li>
    <% end %>
    </ul>
  </div>
<%= link_to 'Add a list', new_list_path(@list), style: "text-decoration:none" %>
<div>
