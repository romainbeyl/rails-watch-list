<h1>New List</h1>
<div class="container">
  <%= simple_form_for [@list] do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.submit "create", class: 'btn btn-primary' %>
  <% end %>
</div>
